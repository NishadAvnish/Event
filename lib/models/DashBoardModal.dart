class DashboardDataModel {
  final String eventName,
      createrName,
      createdTime,
      timeline,
      where,
      description,
      eventImage,
      createrImage;

  DashboardDataModel(this.eventName, this.createrName, this.createdTime,
      this.timeline,this.where, this.description, this.eventImage, this.createrImage);
}
